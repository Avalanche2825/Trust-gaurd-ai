# Node.js server
PORT=4000
NODE_ENV=development
JWT_SECRET=SACHKavach_BharatTrustGrid_2026_SecureKey

# LLM — Primary (xAI Grok) — get at https://console.x.ai
GROK_API_KEY=your_grok_api_key_here

# LLM — Fallback (Groq, free at https://console.groq.com)
GROQ_API_KEY=your_groq_key_here

# Database (optional — system runs on in-memory engine without this)
MONGODB_URI=

# Python ML service (internal)
ML_SERVICE_URL=http://localhost:5001

# Application
APP_URL=http://localhost:4000
